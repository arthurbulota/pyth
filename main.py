class Dog():
        so = 'Suo'
        def __init__(
            self,
            breed,
            name,
            leg_count=4
        ):

        self.leg_count = leg_count
        self.name = name
        if age >= 100:
            print('No go'
        else:
                self.age = age
                )  